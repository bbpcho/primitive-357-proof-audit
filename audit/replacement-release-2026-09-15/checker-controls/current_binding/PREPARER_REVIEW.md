# Parent binding-preparer review

Reviewed `../prepare_current_binding.py`, SHA-256 `689b32b8d0cecb1baeaf8a5d14a2b75fdabe2eb3b1f511369dcb8a20f9ca52f9`.

Its eighteen descriptor roles, finalizer paths, artifact index handling, log-copy root, actual isolated-driver schema, tag/URL construction and prospective status object match the additive helper. It calls the checker with the actual ZIP, so the archive byte count/hash, member set and every payload are revalidated before the candidate overlay is accepted. No prospective availability flag is promoted to the control repository by this preparer.

At the parent's request, the preparer now rejects intersecting final/output roots before creating any output. Its three active isolation roles name CLEAN2's driver, profile and probes. The existing helper is filename-independent and accepts the actual unchanged `primitive357-isolated-driver-v1` schema, including the additional `SDKROOT` environment entry. The actual CLEAN2 preflight has 28 passing checks, including fresh compilation and execution of the exact ordinary-counter source that encountered the host SDK problem in CLEAN1. This establishes preflight compatibility; it does not establish completion of the full mathematical replay.

The actual updated preparer was executed against a compact synthetic complete archive using temporary inputs/outputs under `current_binding/`. It passed, copied 99 required logs, and reported `archive_payloads_checked: true`. A nested output was rejected before directory creation. See `ACTUAL_PREPARER_CHECK.json` and its log. The helper's 33 controls pass under ordinary and optimized Python; these include accepting CLEAN2 while retaining the failed CLEAN1 record separately, and rejecting a failed driver selected as the current driver.

The parent finalizer retains the failed CLEAN1 driver and diagnostic outputs under `records/completed-replay/earlier-incomplete-run/`. These are additional historical records and cannot satisfy the active CLEAN2 driver/result joins. `CLEAN2_SCHEMA_REVIEW.json` records the inspected source/preflight identities.

Only the explicitly authorized preparer guard and three role paths were changed outside this helper directory. No final archive, control repository, original mathematical inputs, or public status was modified. Actual final-archive validation remains pending completion of CLEAN2 and the parent's final seal.
