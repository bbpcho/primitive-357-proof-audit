# Exact geometry supplement for RANK-03 and RANK-05

This bounded review closes the project-specific verification coverage in **RANK-03 and RANK-05**, subject to RANK-01 (the printed curve and its smoothness) and the stated standard bitangent/theta results. It does not close the other rank nodes or certify the entire rank theorem. Original release files and the rank agent's ledger were not modified.

The reviewed source was read equation by equation. The substantive exact tests were then freshly executed on fresh exports of the sealed release's binary/rational data, with only explicit path relocation. A separate new checker binds those exports to one another and identifies every target fibre. Runtime material is in `runtime/rank_geometry/`; `immutable_input_index.json` records 18 sealed input files, `source_index.json` records the initial 13 inspected source files and relocated hashes, and `artifact_index.json` records generated artifacts excluding the Sage cache. `fresh_replay_results.json` records 12 successful export/identity/theta jobs; `fresh_tetrad_replay_results.json` records the additional fresh export and all40,950 conic-rank tests. No saved final success flag was used to accept an identity.

## RANK-03: actual source lines, contact divisors, and target incidence

1. **Exact source and base field.** In the degree56 absolute polynomial quotient, the exported element `s=T5` satisfies `s²+7=0`. The source polynomial is monic, integral and irreducible, freshly checked by Sage in the theta worker. Thus it defines a field L of degree28 over K. Fresh `check_source_base.gp` also verifies that the p5 and V3 copies of the tower have identical P and T5, and `Pre[8]=T5` exactly. `export_exact_tau_data.gp`, lines9–16, binds this P to the line/contact source, the R42 polynomial to the exact norm record, R56 to its rational incidence record, and both copies of H28 to the same record.

   The independent source-quotient check `check_source_quartic_sign.py` verifies all five coefficients of the contact-square identity with the printed curve's **s=+T5**. Replacing s by−T5 fails four of five coefficients. This rules out a hidden conjugation between this geometry and the manuscript's coefficient convention. The later-discovered incorrect “S14” prose at29 is not repaired by changing this sign.

2. **Exact line and contact transfer.** `check_exact_line_bindings.gp`, lines4–7, binds the absolute Horner line coordinates, relative incidence line coordinates, scale `Pre[9]`, and V3 universal line, and checks the conic target modulus. The contact worker, lines11–25, constructs the absolute target polynomial
   `Tabs=R0²+R0 R1+2R1²`, defines `w=−R0/R1`, and checks `w²−w+2=0`, `R0+w R1=0`. Its relative incidence quotient has the monic quartic J as modulus. The line is `Z=−uX−vY`; its contact form is `A X²+B XY+C Y²`.

   The direct source check and contact worker, lines41–51, expand the actual printed quartic into its five coefficients f_i and check
   `f_i A²=f_4 (C²,2BC,B²+2AC,2AB,A²)_i`.
   The source check also verifies `f_4≠0`. Because L is a field, this is a genuine scalar-times-square restriction. The fresh finite reduction has 28 distinct normalized lines, so the 28 conjugates are distinct characteristic-zero bitangents. BPS Definition12.1 and Section12.1 identify these with all28 odd theta characteristics of a smooth plane quartic; they are not an arbitrary 28-element set.

3. **Exact contact conic.** With q0,…,q5 in the usual degree-two monomial order, substituting the same line gives
   `d2=q0−u q2+u²q5`,
   `d1=q1−v q2−u q4+2uv q5`,
   `d0=q3−v q4+v²q5`.
   The exact worker checks `A d1=B d2`, `A d0=C d2`. The separate full-fibre unit check verifies A and d2 nonzero at every one of the168 incident flags at each embedding of q=733177; all rational denominators there are invertible. This supplies the componentwise nonvanishing which a bare `A!=0` assertion in a product algebra would not establish. The conic therefore contains the four contact divisors. Their degrees sum to8, so its intersection divisor on the quartic is exactly that sum. BPS Lemma12.2 supplies the syzygetic interpretation.

4. **Full target and root-order binding.** The new `check_export_and_fibre_bindings.py` proves exact array equality between the contact and tau exports for R42/J42, and between the fresh R56/J56/H28 export and the degree56 trace JSON (including all huge rational coefficients, not only their reductions). At both K embeddings over733177 it verifies that all28 ordered roots equal the roots used by the theta checkpoints. For both target degrees42 and56, it checks the entire resolvent equals the product over its distinct recorded target nodes, and each quartic incidence polynomial equals the product over exactly the four assigned source roots. All196 target fibres pass. All98 masks per embedding are actual syzygetic masks and span the same rank21 space as all315 masks.

   This finite comparison identifies the exact algebraic incidence, because the exact target polynomials are squarefree and the exact J divides the separable H28. The target and source roots in the compared fibre are distinct; uniqueness of their Hensel lifts binds each exact degree-four divisor to its four selected source roots. This does not infer exact polynomial identities merely from congruences: the characteristic-zero divisibility is independently verified first.

5. **Full315 geometry and theta dictionary.** The inspected, freshly rerun conic checker tests all20,475 four-subsets at each embedding. The ranks are315 instances of5 and20,160 instances of6, and the315 masks equal the checkpoint masks. The curve has smooth reduction at this prime (RANK-01), the lines and contact forms have the checked integral reductions, and distinct bitangents specialize injectively. Characteristic-zero syzygies specialize to these conic relations; BPS Section12.2/Proposition5.7 gives exactly315. Thus the entire incidence structure is the actual one. Equivalently, prime-to-characteristic theta relations are preserved under good reduction. This is an ordered proof—source bitangents, then all315 relations, then target identification—not a circular inference from an assumed AGL group.

6. **Exact tau(c).** In `check_exact_tau_sage.py`, lines32–50, P is squarefree, the selected literal CP22 equals the Horner reconstruction from the56 N6 coordinates, and H28 times its quadratic conjugate equals P. The wrong base conjugation fails the reconstruction. For relative target degrees42 and56, lines73–103 independently check squarefreeness of the absolute polynomials of degrees84 and112, reconstruct J, verify J divides H28 by explicit monic long division, substitute **the same literal source c** into the incidence algebra, and compute the determinant of multiplication by that element in basis1,l,l²,l³. The determinant is evaluated by the24-term Leibniz formula and equals the stored target norm in both algebras. Altering the incidence coefficient or the source value is rejected. Since nonzero c lies in the source field, its pullback is a unit in every incidence component; these are the actual four-incidence norms, not merely nonzero elements in a product quotient. This supplies the exact norm premise for the separately reviewed coherent-root argument RANK-25.

## RANK-05: intrinsic theta, exact tower, and AGL containment

The relevant primary statements were checked in BPS: Theorem5.1 identifies theta characteristics with quadratic refinements of the Weil pairing; Proposition5.4 recovers the full theta/symplectic structure from the actual odd-theta incidence; Corollary5.5 identifies the Galois action. See the local primary-text copy `work/v3_audit/bps.txt`, lines333–484, and [BPS](https://math.mit.edu/~poonen/papers/genus3.pdf), Sections5 and12.

- **Actual affine labels.** The intrinsic checker verifies28 distinct zeros of a nondegenerate six-dimensional quadratic form, equality of all315 xor-zero tetrads with the actual conic masks, and full rank6 of theta differences. The newly checked tetrad relation rank is21. Proposition5.4 therefore identifies this finite labeling with the actual theta torsor and its Weil pairing. An arbitrary labeling or status marker is not being substituted for that identification.
- **The seven blocks are intrinsic.** In the exact degree56 field, the tower worker verifies `T1(T4)=0`, `T3(T4)=T5`, and `T5²=−7`, using exactly the source T5 above. T1 is monic of degree14. At both embeddings it verifies seven distinct simple T1 roots, each with four of the28 source roots. The two sets of seven are disjoint because T3 gives the two different K embeddings. These14 distinct conjugates prove that T4 has degree14 over Q and degree7 over K. Hensel uniqueness makes the seven four-element blocks exact fibres of the K-defined tower map. Hence their unordered partition is Galois-stable.
- **The eight candidates are intrinsic.** The rule “an even theta whose translated odd vectors are pairwise Weil-orthogonal in every block” is preserved by Galois. Fresh exhaustive enumeration gives exactly8 such theta characteristics and, for each,56 distinct triples whose theta sum is that candidate. Thus each triangle-sum invariant has degree at most8 over K and at most16 over Q. This degree bound follows from the intrinsic eight-set; it is not assumed from an unproved Galois-group equality.
- **Exact separating invariant.** The worker checks P's irreducibility and the integer inequality
  `9^56−sum_{i<56}|P_i|9^i =1203667875907328603920201507700938464959372265898744 >0`.
  Thus every conjugate of a source root has absolute value less than9. Every triangle sum is an algebraic integer of absolute value at most `56·9³=40824`. For `w=(1+s)/2`, the candidate `−48+12w` is integral and has absolute value less than65 at either archimedean embedding. A nonzero difference therefore has nonzero integer absolute norm bounded by
  `(40824+65)^16 =61052553110133200106762487384127542891703819077749816583455980468086757761`.
  Fresh verification of28 simple roots to precision733177^24 at each embedding, their exact T5 values, and all16 triangle sums proves that candidate0 agrees with `−48+12w` modulo733177^24. This prime power is strictly larger than the norm bound. A nonzero integral difference with this local valuation would have norm divisible by733177^24, contradiction. Hence the equality is exact. All8 invariant residues are distinct, so equality with this K-element fixes the selected theta under Galois.
- **Containment only.** From its56 triangle triples the checker reconstructs8 stars of size7, identifies all28 edges of K8, and verifies the seven blocks are its seven affine parallel matchings. It explicitly enumerates all vertex permutations preserving this data, obtaining exactly the1344 affine transformations of F2³. Since both the selected theta and the partition have been proved Galois-stable, the actual Galois image is contained in this AGL group. Equality with AGL is neither proved nor needed. Irreducibility of P already gives transitivity on the28 actual bitangents.

## Scope and remaining premises

No project-specific input-binding or exact-identity gap remains in these two nodes after the fresh checks above. Standard Sage/PARI exact arithmetic and polynomial irreducibility specifications are trusted; their implementations are not formally verified. Smoothness and the printed coefficient identification remain RANK-01. The coherent choices of square roots, relation signs, global-class support, unit/class-group generation, other local stopping arguments and the final Selmer deduction remain their separate nodes. In particular, this supplement does not turn the success of these geometry checks into a rank upper-bound claim.
