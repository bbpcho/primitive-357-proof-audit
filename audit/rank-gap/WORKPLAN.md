# Completed rank proof and release verification

The R1–R7 mathematical work packages are complete at the combined independent audit boundary. Their concrete argument is in `docs/RANK_GAP.md` and `paper/rank-proof.tex`. The rank package, earlier p=5 proof and earlier saturation/logarithm checks are composed in the new release.

The release replay is a separate final dependency. `audit/status.json` remains pending until the newly assembled candidate has passed all three integrated components and every tested input is bound to the final archive. This distinction preserves the completed mathematical audit without presenting a future replay as an accomplished run.

The index-coprime-to 400 statement belongs to H1, not the original H0. See `docs/H0_H1_CORRIGENDUM.md`.
